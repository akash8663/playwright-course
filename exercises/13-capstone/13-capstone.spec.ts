import { test, expect } from '@playwright/test';

// Sanity check so the suite stays green before you start. Replace/extend below.
test('sanity', async ({ page }) => {
  expect(page).toBeTruthy();
});

// TODO: implement the capstone — see README.md in this folder.
// You'll likely add page-object and fixture files alongside this spec,
// the same shape as Module 8, applied to the full flow below.
