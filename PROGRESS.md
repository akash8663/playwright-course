# Progress

Status per module. Updated by the mentor after each review.

| # | Module | Status | Attempts | Notes |
|---|---|---|---|---|
| 1 | Basics | passed | 1 | Solid first pass — title + heading assertions, web-first `expect`. Coaching note: prefer `getByRole` over tag selectors going forward. |
| 2 | Locators & Selectors | passed | 1 | Correctly triggered and resolved strict-mode ambiguity with role/text locators, no CSS/XPath. Coaching notes: `.first()`/`.nth(0)` are redundant together; avoid actions with no trailing assertion. |
| 3 | Actions & Assertions | passed | 2 | First submission failed lint (unnecessary `await` on locator creation caused `no-unused-locators` false positives) — fixed on resubmit. Good use of a negative assertion to verify checkbox scoping didn't leak between items. Learned `getByRole('listitem')` + string `.filter({hasText})` can replace `page.locator('li'/'label')` entirely. |
| 4 | Navigation & Multiple Pages | passed | 1 | Clean pass-then-click race with `context.waitForEvent('page')`, asserted URL + heading on the new page. Coaching note: initial submission hit a `ReferenceError` from forgetting to destructure `context` in the test's fixture parameters alongside `page` — worth remembering that every fixture (page, context, request, etc.) has to be explicitly requested in `{ }`, it's not auto-injected. |
| 5 | Forms & Input Handling | passed | 1 | Correct `selectOption`/`check`/`uncheck` usage, no stray `await` on locator creation (Module 3 lesson stuck). Coaching notes: mixed CSS ID selectors for dropdowns vs `getByLabel` for checkboxes — prefer role/label consistently; and assert initial state before acting to prove a transition actually happened rather than the control already being there by default. |
| 6 | Network | passed | 2 | First submission missed the "assert the request was actually made" criterion; fixed with `page.waitForRequest` set up before the click (correct race-avoidance ordering, same pattern as Module 4). Coaching note: the trailing `expect(request).toBeTruthy()` is trivially true and flagged by lint's `no-useless-await` — a real request-property assertion (`request.method()`/`request.url()`) would be more meaningful. |
| 7 | Authentication & State | passed | 2 | `.env`/dotenv wiring correct, no hardcoded secrets. Strong test design: asserts blocked-without-auth then works-with-reused-state in one flow, and used the real `context.setStorageState(path)` API (applies state to an existing context, not just at creation) — better than the mentor's own worked example. First submission raced under `fullyParallel: true` (reproduced: state-consuming test ran before state-creating test wrote the file); fixed with `test.describe.configure({ mode: 'serial' })` at the file's top level, verified with 3 fresh reruns (auth file deleted each time). |
| 8 | Test Organization | passed | 1 | `FormPage`/`TodoPage` classes + matching custom fixtures, 2 tests using them. Coaching note: `playwright/expect-expect` initially flagged the todo test since it only asserted indirectly inside `TodoPage` methods (`checkTodoItem`/`verifyTodoItemChecked`) — the rule only recognizes direct `expect()` calls in the test body by default. Discussed two fixes (reconfigure `assertFunctionNames` vs. add a direct assertion) and chose to add a direct `expect()` in the test, keeping the global lint config untouched so the rule's guarantee stays intact for future modules. |
| 9 | Visual & Accessibility | passed | 2 | First submission had both `expect()` calls missing `await` — the un-awaited assertion promise outlived the page teardown, showing up as either "errors not part of any test" (CLI) or `screencast.showOverlays: ...browser has been closed` (VS Code live view) depending on how it was run; same root cause, different messenger. Fixed by awaiting both. Baselines committed for all 3 browser projects; accessibility check correctly uses role+name locator only. |
| 10 | Parallelism & CI | passed | 1 (with debugging) | Workflow correct: triggers on push/PR, Node 22, `npm ci`, browser install, secrets via `${{ secrets.* }}`, artifact upload on failure. Debugged along the way: `continue-on-error: true` on the test step was silently suppressing failure detection (fixed by removing it); Linux-platform screenshot baselines were missing since baselines are OS-specific and the originals were Windows-generated (fixed via Docker + `--update-snapshots`, both `-win32` and `-linux` baselines now committed). Verified green via GitHub Actions API against current HEAD. Minor note: temporary snapshot-generation step left commented out rather than deleted. |
| 11 | Reporting & Integrations (Slack) | passed | 3 | Custom `SlackReporter` implementing Playwright's `Reporter` interface, registered in `playwright.config.ts`'s reporter array, posts pass/fail/duration to a real Slack channel via `onEnd` — verified with a screenshot showing the message matching the exact run's duration. First attempt (submitted as a GitHub Actions workflow step instead of a Playwright reporter) failed: missing `if: always()` (same bug class as Module 10's artifact-upload step — silently only fires on success), no pass/fail/duration data, webhook-only so no local-run support. Rebuilt as a proper custom reporter; second attempt had unawaited `fetch()` in `onEnd` (real race risk — Playwright's `onEnd` return type is deliberately awaitable so async work like this can finish before process exit, same lesson as Module 9's missing-await) plus fragile string-matching for pass/fail counts and a typo (`TotoalPassed`) that would've shipped to the real channel. All fixed on third submission: `onEnd` now `async`/awaited, counts tracked via `result.status` directly. Redundant/still-broken CI-level Slack step commented out (learner's preference over deletion) once the reporter made it obsolete. |
| 12 | Advanced | passed | 2 | First submission's API test had a copy-paste mismatch (fetched `/todos/1` but asserted `id === 2`) — fixed. Flaky-test reflection initially missed the "unfamiliar flaky test" / trace-viewer point entirely; added on resubmit but stayed fairly generic ("I'll look at it in the console") rather than naming specifics (action timeline, before/after DOM snapshots, network log, comparing a passing vs failing trace) — worth sharpening in practice, not blocking. |
| 13 | Capstone | todo | 0 | |

Status values: `todo`, `in-progress`, `passed`, `failed` (failed = most recent submission failed review, revision expected), `deferred` (intentionally skipped, to be revisited before it blocks something else).
